const http = require("node:http");
const os = require("node:os");

function getLocalIP(){
    const networkInterface =  os.networkInterfaces(); // Basically running `ifconfig -a` or `ip address`

    for ()
}

console.log(getLocalIP());
